<Window x:Class="NEManager.App.Views.StreamsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="560" Width="860" MinHeight="380" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDeepBrush}"
        FontFamily="{StaticResource UIFont}"
        Foreground="{StaticResource TextBrush}">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitle}" Text="NTFS 备用数据流 (ADS)"/>
                <TextBlock x:Name="FilePathText" Style="{StaticResource MonoText}"
                           Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardPanel}" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="数据流列表" Style="{StaticResource SectionTitle}" Margin="4,0,0,8"/>
                    <DataGrid Grid.Row="1" x:Name="StreamGrid" SelectionChanged="StreamGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="名称" Binding="{Binding CleanName}" Width="*"/>
                            <DataGridTextColumn Header="大小" Binding="{Binding SizeText}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="12" Background="Transparent"/>

            <Border Grid.Column="2" Style="{StaticResource CardPanel}" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="流内容" Style="{StaticResource SectionTitle}" Margin="4,0,0,8"/>
                    <TextBox Grid.Row="1" x:Name="ContentBox" IsReadOnly="True"
                             Background="{StaticResource BgDarkBrush}"
                             Foreground="{StaticResource TextBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             FontFamily="{StaticResource MonoFont}" FontSize="12.5"
                             TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="8"/>
                </Grid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="读取内容" Style="{StaticResource ToolButton}" Click="View_Click"/>
            <Button Content="新建流" Style="{StaticResource ToolButton}" Click="Create_Click"/>
            <Button Content="导出为文件" Style="{StaticResource ToolButton}" Click="Export_Click"/>
            <Button Content="删除流" Style="{StaticResource DangerButton}" Click="Delete_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="关闭" Style="{StaticResource AccentButton}" Click="Close_Click" MinWidth="90"/>
        </StackPanel>
    </Grid>
</Window>
