<UserControl x:Class="NEManager.App.Views.TextEditorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDeepBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,10" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="打开文件" Style="{StaticResource ToolButton}" Click="Open_Click"/>
                <Button Content="保存" Style="{StaticResource ToolButton}" Click="Save_Click"/>
                <Separator Width="10" Background="Transparent"/>
                <TextBlock Text="编码:" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox x:Name="EncodingSelector" Width="120" SelectionChanged="Encoding_Changed"/>
                <Separator Width="10" Background="Transparent"/>
                <TextBox x:Name="SearchBox" Width="200" VerticalContentAlignment="Center" KeyDown="Search_KeyDown"/>
                <Button Content="查找" Style="{StaticResource ToolButton}" Click="Find_Click"/>
                <Button Content="下一个" Style="{StaticResource ToolButton}" Click="FindNext_Click"/>
                <Separator Width="10" Background="Transparent"/>
                <TextBlock x:Name="LineCountText" VerticalAlignment="Center" Foreground="{StaticResource TextMutedBrush}"/>
            </StackPanel>
        </Border>

        <!-- 文本显示区 -->
        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="LineNumbers" FontFamily="{StaticResource MonoFont}"
                           FontSize="13" Foreground="{StaticResource TextMutedBrush}"
                           Background="{StaticResource BgDarkBrush}" Padding="8,10"
                           TextAlignment="Right" LineHeight="20" MinWidth="50"/>
                <TextBox Grid.Column="1" x:Name="TextContent" FontFamily="{StaticResource MonoFont}"
                         FontSize="13" Foreground="{StaticResource TextBrush}"
                         Background="{StaticResource BgPanelBrush}" BorderThickness="0"
                         IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap"
                         Padding="10" TextChanged="TextContent_TextChanged"/>
            </Grid>
        </Border>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Style="{StaticResource CardPanel}" Margin="0,10,0,0" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Style="{StaticResource StatusText}" Text="未加载文件"/>
                <TextBlock x:Name="EncodingText" Style="{StaticResource StatusText}" Margin="20,0,0,0"
                           Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock x:Name="CursorText" Style="{StaticResource StatusText}" Margin="20,0,0,0"
                           Foreground="{StaticResource TextMutedBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
