<Window x:Class="NEManager.App.Views.TextViewerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="620" Width="880" MinHeight="300" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDeepBrush}"
        FontFamily="{StaticResource UIFont}"
        Foreground="{StaticResource TextBrush}">

    <Grid Margin="14">
        <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" Style="{StaticResource SectionTitle}" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="2">
            <TextBox x:Name="ContentBox"
                     Background="Transparent"
                     Foreground="{StaticResource TextBrush}"
                     BorderThickness="0"
                     IsReadOnly="True"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Padding="10"/>
        </Border>

        <TextBlock Grid.Row="2" x:Name="LineCountText" Style="{StaticResource StatusText}" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="3" x:Name="StatusText" Style="{StaticResource StatusText}" Margin="0,4,0,0" Foreground="{StaticResource SuccessBrush}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="复制到剪贴板" Style="{StaticResource ToolButton}" Click="Copy_Click"/>
            <Button Content="保存为文件" Style="{StaticResource ToolButton}" Click="Save_Click"/>
            <Button Content="关闭" Style="{StaticResource AccentButton}" Click="Close_Click" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>
